[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "monitoring-resilience"
version = "0.1.0"
description = "Salesforce monitoring hardening: retry with exponential backoff + jitter, token-bucket rate limiting (deterministic, stdlib-only)"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["resilience"]